import React from "react";
import styled from "styled-components";

type InputProps = {
  width:string;
  height:string;
};

const ImputsWithIcons = ({ width, height }: InputProps) => {
  return <div></div>;
};

export default ImputsWithIcons;
